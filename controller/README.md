# RK3566 控制服务

计划采用 Python、OpenCV 和 FastAPI，运行于板厂适配的 ARM64 Linux。当前尚无可运行程序。

职责：提供网页和 API、管理任务状态机、采集 USB 相机图像、执行定位算法、与下位机通信。

建立工程时分开组织 API、设备状态机、相机、视觉和下位机适配层。硬件由唯一控制实例持有，避免多个 Web worker 重复打开相机或发送动作。

Python 依赖在目标环境中安装；不能复制笔记本的虚拟环境到板端。服务自启动配置在实际系统确认后补充。
